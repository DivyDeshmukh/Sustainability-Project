import React from "react";

function LogoutBtn() {
  const logout = async () => {};

  return (
    <button className="text-black" onClick={logout}>
      Logout
    </button>
  );
}

export default LogoutBtn;
